server:
  port: 8080
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    com.example.mapper: debug

# spring配置
spring:
  profiles:
    active: "dev"
  datasource:
    password: Zql@20040712
    username: root
    url: jdbc:mysql://localhost:3306/blog?characterEncoding=utf-8&serverTimezone=GMT%2B8
  data:
    redis:
      host: localhost      # 本地开发填 localhost；云服务填控制台给的地址
      port: 6379           # Redis 默认端口
      password: ""         # 本地无密码可空；云服务（如 Azure Redis）必填！
      database: 0          # Redis 有 16 个库（0～15），开发常用 0
      timeout: 2000ms      # 操作超时时间
  # spring email配置，根据配置自动在spring里创建
  mail:
    host: smtp.qq.com
    port: 465
    username: 3100649706@qq.com
    password: jwggashwraezdfdf
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
  # thymeleaf配置
  thymeleaf:
    cache: false  # 开发时关闭缓存
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
  security:
    jwt:
      key: jwt-key
      expire: 7 # 有效时间
  rabbitmq:
    # rabbitmq配置
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        retry:
          enabled: true # 允许消费者重试
          max-attempts: 3 # 最大重试次数
          initial-interval: 6000 # 重试间隔时间
          multiplier: 2 # 乘子*间隔时间=下一次重试时间，不能超过最大间隔时间
          max-interval: 10000ms # 最大间隔时间
    # 信息配置
    # 交换机，相当于分拣中心
    exchange:
      email: email_exchange
      log: log_exchange
    # 队列，相当于仓库货架
    queue:
      email: email_queue
      log-login: log_login_queue
      log-system: log_system_queue
    # 路由健，相当于货架上的标签
    routingKey:
      log-login: log_routing_key_login
      log-system: log_routing_key_system
      email: email_routing_key

# 邮件配置
mail:
  link:
    apply:
      redirect-uri: http://localhost:8080/link/email/apply
      # 邮箱审核友链回调地址（如果不开启）apply-notice 则无需在意该配置，随便填写即可
      # redirect-uri: http://[(后端地址:端口)/(域名)]/link/email/apply
  article-email-notice: false
  message-email-notice: false
  article-reply-notice: false
  message-reply-notice: false

# 对象存储配置
minio:
  bucketName: bn
  endpoint: http://localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin

web:
  index:
    # 网站前端首页
    path: http://localhost:70/
